<%= turbo_stream.prepend "comments_list" do %>
  <%= render @comment %>
<% end %>

<%= turbo_stream.replace "new_comment" do %> 
  <%= render "comments/form", portfolio: @portfolio, comment: Comment.new %>
<% end %>
